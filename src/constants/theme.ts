const COLORS = {
    //primary_light: "#53dce3",
    primary: "#3F51B5",
    //primary_dark: "#21aeb4",

    grey:"#707070" ,
    light_grey:"#F5F5F5" ,

    white: "#fcfcfc",

    black: "#080808"
};

const SIZES = {
    pressable: {
        defaultWidth: 350,
        defaultBorderRadius: 20,
        defaultPadding: 12,
        defaultBorderWidth: 1,
        defaultMargin: 10
    }
}

const FONT_SIZES = {
    xSmall: 10,
    small: 12,
    medium: 16,
    large: 20,
    xLarge: 24,
    xxLarge: 32,
};



/*const STYLE ={
    defaultFieldMargin: 15,
    defaultButton: {
        padding: FONT_SIZES.small,
        backgroundColor: COLORS.primary_light,
        borderRadius: FONT_SIZES.large,
        width: SIZES.pressable.defaultWidth
    },
    defaultButtonText: {
        color: COLORS.white
    },
    defaultButton2: {
        padding: FONT_SIZES.small,
        backgroundColor: COLORS.primary,
        borderRadius: FONT_SIZES.large,
        width: SIZES.pressable.defaultWidth,
        borderWidth: 2,
        borderColor: COLORS.primary_light
    },
    defaultButtonText2: {
        color: COLORS.primary_light
    },
    defaultLinkText: {
        color: COLORS.light_grey
    },
    defaultPlaceForText: {
        padding: FONT_SIZES.small,
        backgroundColor: COLORS.primary_light,
        borderRadius: FONT_SIZES.small,
        width: SIZES.pressable.defaultWidth,
        borderWidth: 1,
        borderColor: COLORS.grey
    },
    defaultBigText: {
        color: COLORS.black,
        fontSize: 20
    },
}*/

export {COLORS, FONT_SIZES, SIZES}
